# 👁️ 云岭天眼 — 技术文档

> 高原特色农产品区块链溯源系统  
> 基于 Hyperledger Fabric v2.5.10  
> 文档版本: v1.0 | 2026-06-27

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术栈与框架](#2-技术栈与框架)
3. [Hyperledger Fabric 架构详解](#3-hyperledger-fabric-架构详解)
4. [共识机制实现原理](#4-共识机制实现原理)
5. [六大智能合约设计](#5-六大智能合约设计)
6. [多组织协同流程](#6-多组织协同流程)
7. [后端架构设计](#7-后端架构设计)
8. [前端架构设计](#8-前端架构设计)
9. [搭建过程与踩坑记录](#9-搭建过程与踩坑记录)
10. [教师可能提问的问题集](#10-教师可能提问的问题集)
11. [术语表](#11-术语表)

---

## 1. 系统概述

### 1.1 项目背景

云岭天眼是一个聚焦云南高原特色农业的农产品溯源系统，基于 Hyperledger Fabric 联盟链技术，实现从产地种植、检测认证、冷链物流到销售交易的全链路数据上链与可信追溯。

### 1.2 核心目标

- **可信溯源**：利用区块链不可篡改特性，确保农产品溯源数据真实可靠
- **多组织协同**：农业局、检测中心、供应链平台三个组织分工协作
- **多合约架构**：6 大智能合约覆盖溯源全生命周期
- **云南特色**：聚焦普洱茶、小粒咖啡、野生菌等 49+ 种高原特色农产品

### 1.3 系统架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                        前端 (Vue 3 + Vite)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │  Home    │  │Agriculture│  │Inspection│  │ SupplyChain  │ │
│  │ (首页)   │  │ (农业局)  │  │ (检测中心)│  │ (供应链平台) │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐│
│  │              BlockchainViz (链上可视化)                   ││
│  └──────────────────────────────────────────────────────────┘│
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP /api/*
┌──────────────────────────▼───────────────────────────────────┐
│                   后端 (Go + Gin)                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ Handler  │→│ Service  │→│  Fabric   │→│  Block       │ │
│  │ (API层)  │  │ (业务层) │  │ Gateway  │  │  Listener    │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘ │
└──────────────────────────┬───────────────────────────────────┘
                           │ gRPC
┌──────────────────────────▼───────────────────────────────────┐
│              Hyperledger Fabric 区块链网络                    │
│  ┌──────────────────────────────────────────────────────────┐│
│  │                Orderer 集群 (Raft 共识)                   ││
│  │     orderer1.togettoyou.com :7050                        ││
│  │     orderer2.togettoyou.com :8050                        ││
│  │     orderer3.togettoyou.com :9050                        ││
│  └──────────────────────────────────────────────────────────┘│
│                           │                                   │
│    ┌──────────────────────┼──────────────────────┐           │
│    ▼                      ▼                      ▼           │
│ ┌──────────┐        ┌──────────┐          ┌──────────┐      │
│ │  Org1    │        │  Org2    │          │  Org3    │      │
│ │ 农业局   │        │ 检测中心 │          │供应链平台│      │
│ │ Org1MSP  │        │ Org2MSP  │          │ Org3MSP  │      │
│ │ peer0    │        │ peer0    │          │ peer0    │      │
│ │ peer1    │        │ peer1    │          │ peer1    │      │
│ └──────────┘        └──────────┘          └──────────┘      │
│        │                  │                     │            │
│        └──────────────────┼─────────────────────┘            │
│                           ▼                                   │
│              ┌──────────────────────┐                        │
│              │   mychannel 通道      │                        │
│              │   分布式账本 + 链码   │                        │
│              └──────────────────────┘                        │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. 技术栈与框架

### 2.1 技术栈总览

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **区块链** | Hyperledger Fabric | v2.5.10 | 联盟链底层框架 |
| **链码语言** | Go (contract-api) | 1.23+ | 智能合约开发 |
| **后端框架** | Gin | v1.10.0 | Go Web 框架 |
| **区块链 SDK** | fabric-gateway | v1.7.0 | Go 与 Fabric 交互 |
| **协议** | fabric-protos-go-apiv2 | v0.3.4 | Fabric 协议支持 |
| **前端框架** | Vue 3 | v3.3.8 | 渐进式前端框架 |
| **语言** | TypeScript | v5.0.2 | 类型安全的 JS 超集 |
| **构建工具** | Vite | v4.5.0 | 前端构建与热更新 |
| **UI 库** | Ant Design Vue | v3.2.20 | 企业级 UI 组件库 |
| **状态管理** | Pinia | v2.1.7 | Vue 状态管理 |
| **路由** | Vue Router | v4.2.5 | 前端路由 |
| **HTTP 客户端** | Axios | v1.6.2 | HTTP 请求 |
| **区块存储** | BoltDB | v1.3.11 | 嵌入式 KV 数据库 |
| **容器化** | Docker / Docker Compose | v24+ | 环境部署 |
| **网络** | Fabric 网络 | 3 Orderer + 6 Peer | 三组织六节点 |

### 2.2 为什么选择 Hyperledger Fabric

| 特性 | Fabric | 公链 (ETH) | 说明 |
|------|--------|------------|------|
| 联盟治理 | ✅ 原生支持 | ❌ 无许可 | 三组织分级权限 |
| 身份认证 | ✅ MSP + PKI | ❌ 匿名 | 每个操作都有身份背书 |
| 交易隐私 | ✅ 通道隔离 | ❌ 公开 | 数据仅对授权组织可见 |
| 共识效率 | ✅ 高 (Raft CFT) | ❌ 低 (PoW/PoS) | 毫秒级确认 |
| 智能合约 | ✅ 通用语言 | ❌ Solidity | Go/Java/Node.js |
| 无代币 | ✅ 无需 Gas | ❌ 需代币 | 企业友好 |

### 2.3 为什么选择 Go + Gin

- **高性能**：Go 编译型语言，性能接近 C
- **并发模型**：Goroutine 天然适合高并发 API 服务
- **Fabric SDK**：fabric-gateway 原生 Go SDK
- **快速开发**：Gin 框架路由简洁，中间件丰富
- **部署简单**：编译为单一二进制文件

### 2.4 为什么选择 Vue 3 + Vite

- **组合式 API**：Vue 3 Composition API 逻辑复用更灵活
- **TypeScript**：类型安全，减少运行时错误
- **Vite HMR**：极速热更新，开发体验好
- **Ant Design Vue**：成熟的企业级组件库
- **生态系统**：Pinia + Vue Router 官方推荐

---

## 3. Hyperledger Fabric 架构详解

### 3.1 Fabric 核心组件

#### 3.1.1 Peer 节点 (Peer)

Peer 节点是 Fabric 网络的基础节点，负责：
- **存储账本**：维护区块链账本和世界状态
- **执行链码**：模拟执行交易（背书阶段）
- **验证交易**：验证区块中的交易合法性
- **提交区块**：将验证通过的区块追加到本地账本

本系统共 6 个 Peer 节点，每个组织 2 个：
```
Org1: peer0.org1.togettoyou.com:7051, peer1.org1.togettoyou.com:17051
Org2: peer0.org2.togettoyou.com:27051, peer1.org2.togettoyou.com:37051  
Org3: peer0.org3.togettoyou.com:47051, peer1.org3.togettoyou.com:57051
```

多 Peer 设计目的：
- **高可用**：单个 Peer 宕机不影响组织参与网络
- **负载均衡**：多个 Peer 分担交易验证请求
- **数据冗余**：每个 Peer 保存完整的账本副本

#### 3.1.2 Orderer 节点 (排序节点)

Orderer 节点负责：
- **交易排序**：将交易按时间顺序排序
- **区块打包**：将一批交易打包成区块
- **共识达成**：通过 Raft 协议达成排序共识
- **区块分发**：将新区块分发给所有 Peer

本系统 3 个 Orderer 节点：
```
orderer1.togettoyou.com:7050
orderer2.togettoyou.com:8050
orderer3.togettoyou.com:9050
```

#### 3.1.3 通道 (Channel)

通道是 Fabric 中的私有通信机制：
- 本系统使用 **mychannel** 作为业务通道
- 所有三个组织加入同一通道
- 通道内的数据仅对加入的组织可见
- 通道拥有独立的账本和链码

#### 3.1.4 MSP (Membership Service Provider)

MSP 管理组织身份和权限：
- **Org1MSP**：农业局——农场注册、产品管理
- **Org2MSP**：检测中心——质量检测、认证
- **Org3MSP**：供应链平台——物流、交易

每个 MSP 包含：
- CA 证书（ca.crt）
- TLS CA 证书（tlsca.crt）
- 管理员证书（Admin@cert）
- 节点证书和私钥

#### 3.1.5 链码 (Chaincode/Smart Contract)

链码是 Fabric 中的智能合约：
- 用 Go 语言编写
- 运行在 Docker 容器中（与 Peer 隔离）
- 通过 contract-api 框架实现
- 本系统包含 7 个合约 struct（6 业务 + 1 工具）

### 3.2 Fabric 网络拓扑

```
                    ┌──────────────────────┐
                    │   CLI 管理容器         │
                    │  cli.togettoyou.com   │
                    └──────────────────────┘
                              │
     ┌────────────────────────┼────────────────────────┐
     │                        │                        │
┌────▼────────────────┐ ┌─────▼────────────────┐ ┌─────▼────────────────┐
│  Orderer Cluster    │ │  ┌──────────────┐   │ │  Channel             │
│  (Raft Consensus)   │ │  │  mychannel    │   │ │  ┌──────────────┐   │
│                     │ │  │  ┌────────┐  │   │ │  │  Chaincode   │   │
│ orderer1←→orderer2  │ │  │  │Ledger  │  │   │ │  │  FarmContract│   │
│     ↕        ↕      │ │  │  │(World  │  │   │ │  │ProductContr.│   │
│ orderer3────────────┘ │  │  │ State) │  │   │ │  │InspectionC. │   │
│                       │  │  └────────┘  │   │ │  │LogisticsC.  │   │
│                       │  └──────────────┘   │ │  │TraceContract│   │
│                       │                     │ │  │TradeContract│   │
│                       │                     │ │  └──────────────┘   │
│                       │                     │ └─────────────────────┘
└───────────────────────┘ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```

### 3.3 Fabric 关键概念对比

| 概念 | Fabric | 传统公链 | 本系统应用 |
|------|--------|----------|-----------|
| 节点身份 | MSP/X.509 证书 | 钱包地址 | 各组织独立证书体系 |
| 共识机制 | Raft (CFT) | PoW/PoS | 3 Orderer 投票 |
| 智能合约 | Docker 隔离运行 | EVM | 6 个独立合约 |
| 交易流程 | Execute-Order-Validate | Order-Execute | 背书→排序→验证→提交 |
| 数据存储 | LevelDB/CouchDB | Merkle Patricia Trie | LevelDB |
| 权限控制 | 细粒度策略 | 全局公开 | 按组织 MSP 限制 |

---

## 4. 共识机制实现原理

### 4.1 什么是共识机制

共识机制是区块链网络中多个节点就交易顺序和数据状态达成一致的算法。在 Fabric 中，共识机制由 Orderer 节点实现。

### 4.2 Fabric 共识演进

| 版本 | 共识机制 | 特点 |
|------|----------|------|
| v1.0 | Solo | 单节点排序，仅用于开发测试 |
| v1.1 | Kafka | 基于 ZooKeeper + Kafka，高吞吐 |
| v1.4.3 | Raft | 内置 etcd/Raft，无需外部依赖 |
| v2.5+ | Raft (生产推荐) | 稳定、易运维、CFT |

### 4.3 Raft 共识协议详解

#### 4.3.1 核心概念

Raft 是一种**崩溃容错（CFT, Crash Fault Tolerant）**共识算法，由 Diego Ongaro 在 2013 年提出。它将共识问题分解为三个子问题：

1. **领导者选举（Leader Election）**
2. **日志复制（Log Replication）**
3. **安全性（Safety）**

#### 4.3.2 节点状态

Raft 中的节点有三种状态：

```
                     ┌──────────┐
          超时+选举   │          │   发现更高任期
      ┌─────────────▶│ Follower │◀──────────────────┐
      │              │ (跟随者) │                    │
      │              └─────┬────┘                    │
      │                    │ 获得大多数投票           │
      │                    ▼                          │
      │              ┌──────────┐                    │
      │              │ Leader   │                    │
      │              │ (领导者) │────────────────────┘
      │              └──────────┘  发现更高任期
      │                    │
      │                    │ 发送日志
      │                    ▼
      │              ┌──────────┐
      └──────────────│ Candidate│
                     │ (候选者) │
                     └──────────┘
```

- **Follower**：默认状态，接收来自 Leader 的心跳和日志
- **Candidate**：发起选举时的临时状态
- **Leader**：负责处理所有客户端请求和日志复制

#### 4.3.3 领导者选举过程

本系统 3 个 Orderer 节点选举流程：

```
时间轴:

1. 初始状态: [orderer1:Follower] [orderer2:Follower] [orderer3:Follower]

2. 选举超时（~150-300ms 随机）:
   orderer2 先超时 → term=1 → 成为 Candidate
   
3. 发起投票:
   orderer2: "请投票给我，term=1，最新日志索引=5"
   
4. 投票响应:
   orderer1: "同意"  ✅
   orderer3: "同意"  ✅  (超过半数 2/3)

5. 成为 Leader:
   orderer2: "我是新 Leader，term=1"
   发送心跳维持权威
```

**关键参数**：
- **选举超时**：150-300ms（随机化避免同时选举）
- **心跳间隔**：50ms（Leader 发送）
- **多数派**：N/2 + 1 = 2（3 节点集群）

#### 4.3.4 日志复制流程

```
客户端提交交易 → Leader
    │
    ▼
Leader 创建日志条目 (term, index, data)
    │
    ├──→ AppendEntries RPC 发送给 Follower1
    └──→ AppendEntries RPC 发送给 Follower2
    │
    ▼
Follower 接收并存储日志，回复成功
    │
    ▼
Leader 收到多数回复 (2/3) → 日志已提交(committed)
    │
    ├──→ 将日志应用到状态机
    ├──→ 通知 Follower1 提交
    └──→ 通知 Follower2 提交
    │
    ▼
区块打包完成，分发到所有 Peer
```

#### 4.3.5 Raft 在本系统的配置

```yaml
# docker-compose-base.yaml 中的 Raft 配置
Consensus:
  Type: "etcdraft"  # 使用 etcd 实现的 Raft
  RaftConfig:
    SnapshotInterval: 10000           # 快照间隔（区块数）
    TickInterval: 500ms               # 时钟滴答间隔
    ElectionTick: 10                  # 选举超时 = 10 * 500ms = 5s
    HeartbeatTick: 1                  # 心跳间隔 = 1 * 500ms = 500ms
    MaxInflightBlocks: 5              # 最大在途区块数
```

### 4.4 Fabric 交易流程（Execute-Order-Validate）

这是 Fabric 区别于其他区块链的核心设计——**背书-排序-验证**三阶段模型：

```
阶段一：执行 (Execute) —— 在 Peer 上模拟执行
阶段二：排序 (Order) —— Orderer 集群共识排序
阶段三：验证 (Validate) —— Peer 验证并提交
```

#### 4.4.1 完整交易生命周期

```
以"农业局注册农场"为例：

┌─────────────────────────────────────────────────────────────────────┐
│ 步骤 1: 客户端提交交易提案 (Proposal)                                │
│                                                                     │
│  客户端(SDK) ──→ Org1.peer0                                         │
│  请求: FarmContract:RegisterFarm(农场数据)                           │
│  签名: 客户端私钥签名                                                │
└──────────────────────────────┬──────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 步骤 2: Peer 背书 (Endorsement)                                     │
│                                                                     │
│  Org1.peer0:                                                        │
│  1. 验证客户端签名 ✔                                                │
│  2. 检查MSP权限: clientMSPID == Org1MSP? ✔                          │
│  3. 模拟执行链码: 在读写集中记录操作                                 │
│  4. 生成背书签名: peer 私钥签名                                      │
│  5. 返回提案响应 ──→ 客户端                                         │
└──────────────────────────────┬──────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 步骤 3: 客户端收集背书并提交交易 (Transaction Submission)             │
│                                                                     │
│  客户端:                                                             │
│  1. 验证背书签名 ✔                                                   │
│  2. 检查背书策略是否满足 (至少 Org1 背书) ✔                          │
│  3. 构造交易 (背书+读写集) ──→ Orderer                               │
└──────────────────────────────┬──────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 步骤 4: Orderer 排序 (Consensus)                                    │
│                                                                     │
│  Orderer 集群:                                                       │
│  1. 接收交易并验签 ✔                                                 │
│  2. Raft 共识排序: 确定交易顺序                                      │
│  3. 超时或达到最大交易数 → 打包区块                                  │
│  4. 分发区块到所有 Peer                                              │
└──────────────────────────────┬──────────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 步骤 5: Peer 验证并提交 (Validation & Commit)                        │
│                                                                     │
│  所有组织的 Peer:                                                    │
│  1. 验证区块结构 ✔                                                   │
│  2. 验证每笔交易的背书签名 ✔                                         │
│  3. 检查读写集冲突 (MVCC检查) ✔                                      │
│  4. 标记交易为 VALID 或 INVALID                                      │
│  5. 将区块追加到本地链                                               │
│  6. 更新世界状态 (World State)                                       │
│  7. 触发区块事件通知                                                 │
└─────────────────────────────────────────────────────────────────────┘
```

#### 4.4.2 背书策略

本系统的背书策略设计：

| 合约方法 | 背书策略 | 说明 |
|----------|----------|------|
| `FarmContract:RegisterFarm` | Org1 必须背书 | 仅农业局可注册农场 |
| `InspectionContract:CreateInspection` | Org2 必须背书 | 仅检测中心可创建报告 |
| `ProductContract:RegisterProduct` | Org1 或 Org3 背书 | 农业局或供应链平台 |
| `TraceContract:GetFullTraceability` | 任一组织背书 | 所有组织可查询 |

### 4.5 Raft vs 其他共识机制对比

| 特性 | Raft (Fabric) | PoW (Bitcoin) | PoS (Ethereum) | PBFT |
|------|--------------|---------------|----------------|------|
| 类型 | CFT | BFT | BFT | BFT |
| 容错能力 | 少数节点宕机 | 50%算力攻击 | 51%质押攻击 | 1/3 拜占庭 |
| 确认时间 | 毫秒~秒级 | ~10分钟 | ~12秒 | 秒级 |
| 吞吐量 | 万级 TPS | ~7 TPS | ~30 TPS | 千级 |
| 能源消耗 | 极低 | 极高 | 中等 | 低 |
| 节点数量 | 几十个 | 无限 | 无限 | 几十个 |
| 最终性 | 立即最终 | 概率最终 | 概率最终 | 立即最终 |
| 适用场景 | 联盟链/企业 | 公链 | 公链 | 联盟链 |

### 4.6 为什么 Fabric 选择 Raft 而非 PBFT

1. **实现简单**：Raft 比 PBFT 更容易理解和实现
2. **无需拜占庭容错**：联盟链的节点是可信的，无需容忍恶意行为
3. **性能足够**：3-7 节点的 Orderer 集群，Raft 性能完全满足
4. **运维友好**：Leader 选举自动完成，故障恢复简单
5. **成熟度高**：etcd/Raft 是业界广泛使用的生产级实现

---

## 5. 六大智能合约设计

### 5.1 合约架构

```
┌────────────────────────────────────────────────────────────────┐
│                      chaincode.go                               │
│                                                                 │
│  BaseContract (基础合约 - 通用方法)                              │
│  ├── getClientMSPID()    — 获取调用者身份                        │
│  ├── getCompositeKey()   — 创建复合键                           │
│  ├── getState()          — 读取状态                             │
│  ├── putState()          — 写入状态                             │
│  └── paginateQuery()     — 分页查询                             │
│                                                                 │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │ FarmContract │ │  Product     │ │  Inspection  │            │
│  │ (产地合约)   │ │  Contract    │ │  Contract    │            │
│  │              │ │  (产品合约)  │ │  (检测合约)  │            │
│  │ Org1调用     │ │  Org1/Org3   │ │  Org2调用    │            │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘            │
│         │                │                │                     │
│  ┌──────┴───────┐ ┌──────┴───────┐ ┌──────┴───────┐            │
│  │ Logistics    │ │  Trace       │ │  Trade       │            │
│  │ Contract     │ │  Contract    │ │  Contract    │            │
│  │ (物流合约)   │ │  (溯源合约)  │ │  (交易合约)  │            │
│  │ Org3调用     │ │  所有组织    │ │  Org3调用    │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│                                                                 │
│  BlockQueryContract (区块查询合约 - 所有组织可调用)              │
└────────────────────────────────────────────────────────────────┘
```

### 5.2 合约数据模型

#### Farm（农场）

```go
type Farm struct {
    ID         string     // UUID
    Name       string     // 农场名称
    Province   string     // 省份
    City       string     // 城市
    District   string     // 区县
    Address    string     // 详细地址
    Area       float64    // 面积（亩）
    OwnerName  string     // 负责人
    OwnerPhone string     // 联系电话
    CertLevel  string     // 认证等级：无公害/绿色/有机
    Status     string     // ACTIVE / SUSPENDED
    CreateTime time.Time
    UpdateTime time.Time
}
```

#### Product（农产品）

```go
type Product struct {
    ID           string   // UUID
    Name         string   // 产品名称
    Category     string   // 类别
    FarmID       string   // 关联农场ID
    FarmName     string   // 农场名称
    BatchNo      string   // 批次号
    Quantity     float64  // 数量
    Unit         string   // 单位
    PlantingDate time.Time // 种植日期
    HarvestDate  time.Time // 采收日期
    Status       string   // 产品状态
    Description  string   // 描述
    CreateTime   time.Time
    UpdateTime   time.Time
}
```

#### InspectionReport（检测报告）

```go
type InspectionReport struct {
    ID               string   // UUID
    ProductID        string   // 关联产品ID
    ProductName      string   // 产品名称
    Inspector        string   // 检测人员
    InspectorOrg     string   // 检测机构
    InspectionDate   time.Time // 检测日期
    Result           string   // PASS / FAIL
    Grade            string   // 等级
    CertNumber       string   // 认证编号
    PesticideResidue string   // 农残检测
    HeavyMetal       string   // 重金属检测
    Microorganism    string   // 微生物检测
    Conclusion       string   // 结论
    CreateTime       time.Time
    UpdateTime       time.Time
}
```

### 5.3 合约方法概览

| 合约 | 方法 | 参数 | 权限 |
|------|------|------|------|
| **FarmContract** | `RegisterFarm` | id, name, province, city, district, address, area, ownerName, ownerPhone, certLevel, createTime | Org1MSP |
| | `UpdateFarm` | 同上 (不含 id) + updateTime | Org1MSP |
| | `SuspendFarm` | id, updateTime | Org1MSP |
| | `QueryFarm` | id | 所有 |
| | `QueryFarmList` | pageSize, bookmark, status | 所有 |
| **ProductContract** | `RegisterProduct` | id, name, category, farmId, farmName, batchNo, quantity, unit, plantingDate, description, createTime | Org1/Org3 |
| | `UpdateProductStatus` | id, newStatus, updateTime | Org1/Org3 |
| | `RecordHarvest` | id, harvestDate | Org1/Org3 |
| | `QueryProduct` | id | 所有 |
| | `QueryProductList` | pageSize, bookmark, status | 所有 |
| | `QueryProductsByFarm` | farmId | 所有 |
| **InspectionContract** | `CreateInspection` | id, productId, productName, inspector, inspectorOrg, inspectionDate, pesticideResidue, heavyMetal, microorganism, conclusion, certNumber, updateTime | Org2MSP |
| | `FailInspection` | id, productId, ..., conclusion, updateTime | Org2MSP |
| | `QueryInspection` | id | 所有 |
| | `QueryInspectionList` | pageSize, bookmark, result | 所有 |
| | `QueryCertificates` | pageSize, bookmark | 所有 |
| **LogisticsContract** | `CreateLogisticsRecord` | id, productId, ..., transporter, transportMode, temperature, humidity, startTime, createTime | Org3MSP |
| | `UpdateLogisticsStatus` | id, newStatus, updateTime | Org3MSP |
| | `QueryLogisticsRecord` | id | 所有 |
| | `QueryLogisticsList` | pageSize, bookmark, status | 所有 |
| | `QueryLogisticsByProduct` | productId | 所有 |
| **TraceContract** | `GetFullTraceability` | productId | 所有 |
| | `VerifyProductByBatch` | batchNo | 所有 |
| **TradeContract** | `CreatePurchaseOrder` | id, productId, ..., buyerName, buyerOrg, sellerName, sellerOrg, totalPrice, createTime | Org3MSP |
| | `UpdateOrderStatus` | id, newStatus, updateTime | Org3MSP |
| | `QueryPurchaseOrder` | id | 所有 |
| | `QueryPurchaseOrderList` | pageSize, bookmark, status | 所有 |

### 5.4 合约调用方式

Fabric v2.x 多合约调用格式：

```
# 方式一: 指定合约名 (推荐)
peer chaincode invoke -n mychaincode -c '{"Args":["FarmContract:RegisterFarm", ...]}'

# 方式二: 调用默认合约
peer chaincode invoke -n mychaincode -c '{"Args":["RegisterFarm", ...]}'
```

注：`contractapi.NewChaincode()` 中第一个传入的合约 struct 为默认合约。本系统中 `FarmContract` 是默认合约。

---

## 6. 多组织协同流程

### 6.1 组织职责分工

| 角色 | MSP ID | Peer 节点 | 智能合约 | 核心操作 |
|------|--------|-----------|----------|----------|
| 🌿 **农业局** | Org1MSP | peer0.org1, peer1.org1 | FarmContract, ProductContract | 注册农场、注册产品、记录采收、溯源查询 |
| 🔬 **检测中心** | Org2MSP | peer0.org2, peer1.org2 | InspectionContract | 创建检测报告、认证评级、证书颁发 |
| 📦 **供应链平台** | Org3MSP | peer0.org3, peer1.org3 | LogisticsContract, TradeContract, ProductContract | 物流追踪、采购订单、产品查询 |

### 6.2 完整业务协同流程

以"普洱茶从茶园到茶杯"为例：

```
时间线            🌿 农业局              🔬 检测中心           📦 供应链平台
                   (Org1MSP)             (Org2MSP)            (Org3MSP)
                   │                      │                     │
1. 注册农场        │                      │                     │
   FarmContract    │──RegisterFarm()──▶    │                     │
   :RegisterFarm  │  写入区块 #N          │                     │
                   │◀──查询FarmList────   │                     │
                   │                      │                     │
2. 注册产品        │                      │                     │
   ProductContract │──RegisterProduct()─▶  │                     │
   :RegisterProduct│  写入区块 #N+1        │                     │
                   │                      │                     │
3. 记录采收        │                      │                     │
   ProductContract │──RecordHarvest()──▶   │                     │
   :RecordHarvest  │  状态: PLANTED→HARVESTED                   │
                   │                      │                     │
4. 检测认证        │                      │                     │
   Inspection      │  查询产品ID           │                     │
   Contract        │◀────QueryProduct──── │                     │
   :CreateInsp.    │                      │──CreateInspection()▶│
                   │                      │  写入区块 #N+2      │
                   │                      │  产品→CERTIFIED     │
                   │                      │                     │
5. 物流运输        │                      │                     │
   Logistics       │                      │                     │
   Contract        │                      │  CreateLogistics()  │
   :CreateLog      │                      │◀────────────────────│
                   │                      │  写入区块 #N+3      │
                   │                      │  产品→SHIPPING      │
                   │                      │                     │
6. 物流送达        │                      │                     │
   Logistics       │                      │                     │
   Contract        │                      │  UpdateLogistics()  │
   :UpdateStatus   │                      │◀────────────────────│
                   │                      │  写入区块 #N+4      │
                   │                      │  产品→SOLD          │
                   │                      │                     │
7. 采购订单        │                      │                     │
   TradeContract   │                      │                     │
   :CreatePO       │                      │  CreatePurchase()   │
                   │                      │◀────────────────────│
                   │                      │  写入区块 #N+5      │
                   │                      │                     │
8. 溯源查询        │                      │                     │
   TraceContract   │                      │                     │
   :GetFullTrace   │──QueryTrace(ID)──▶    │──QueryTrace(ID)──▶ │
                   │  返回完整溯源信息     │  返回完整溯源信息   │
```

### 6.3 数据一致性保证

区块链网络通过以下机制保证所有组织数据一致：

1. **相同账本**：所有 Peer 保存同一通道的完整账本副本
2. **相同链码**：所有组织安装并批准同一版本的链码
3. **序列化交易**：Orderer 确保交易全局有序
4. **原子提交**：区块中所有交易要么全部提交，要么全部回滚
5. **世界状态同步**：提交区块时更新世界状态，保持各节点一致

### 6.4 权限控制矩阵

| 操作 \ 组织 | 农业局(Org1) | 检测中心(Org2) | 供应链(Org3) |
|-------------|:-----------:|:--------------:|:------------:|
| 注册农场 | ✔️ | ❌ | ❌ |
| 更新农场 | ✔️ | ❌ | ❌ |
| 注册产品 | ✔️ | ❌ | ✔️ |
| 记录采收 | ✔️ | ❌ | ✔️ |
| 创建检测报告 | ❌ | ✔️ | ❌ |
| 创建物流记录 | ❌ | ❌ | ✔️ |
| 更新物流状态 | ❌ | ❌ | ✔️ |
| 创建采购订单 | ❌ | ❌ | ✔️ |
| 溯源查询 | ✔️ | ✔️ | ✔️ |
| 查询任意数据 | ✔️ | ✔️ | ✔️ |

---

## 7. 后端架构设计

### 7.1 后端分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  Handler 层 (API层)                                          │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐  │
│  │Agriculture     │ │Inspection      │ │SupplyChain     │  │
│  │Handler         │ │Handler         │ │Handler         │  │
│  │  POST /farm    │ │  POST /report  │ │  POST/logistics│  │
│  │  GET /product  │ │  GET /cert     │ │  POST /order   │  │
│  │  GET /trace    │ │  GET /block    │ │  GET /trace    │  │
│  └────────┬───────┘ └────────┬───────┘ └────────┬───────┘  │
└───────────┼──────────────────┼──────────────────┼──────────┘
            │                  │                  │
┌───────────▼──────────────────▼──────────────────▼──────────┐
│  Service 层 (业务逻辑层)                                     │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐  │
│  │Agriculture     │ │Inspection      │ │SupplyChain     │  │
│  │Service         │ │Service         │ │Service         │  │
│  │ 参数处理        │ │  参数处理       │ │  参数处理       │  │
│  │ 时间格式化      │ │  时间格式化     │ │  时间格式化     │  │
│  │ 错误包装        │ │  错误包装       │ │  错误包装       │  │
│  │ JSON解析        │ │  JSON解析       │ │  JSON解析       │  │
│  └────────┬───────┘ └────────┬───────┘ └────────┬───────┘  │
└───────────┼──────────────────┼──────────────────┼──────────┘
            │                  │                  │
┌───────────▼──────────────────▼──────────────────▼──────────┐
│  Fabric 层 (区块链交互层)                                    │
│  ┌──────────────────────────────┐  ┌──────────────────────┐ │
│  │  fabric-gateway SDK          │  │  Block Listener       │ │
│  │  ├── InitFabric()            │  │  ├── 区块事件监听     │ │
│  │  ├── GetContract(orgName)    │  │  ├── BoltDB 存储      │ │
│  │  ├── SubmitTransaction()     │  │  ├── GetBlocksByOrg() │ │
│  │  └── EvaluateTransaction()   │  │  └── GetBlockByNum()  │ │
│  └──────────────────────────────┘  └──────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐│
│  │  gRPC 连接 (TLS 加密)                                    ││
│  │  ├── org1 → peer0.org1.togettoyou.com:7051               ││
│  │  ├── org2 → peer0.org2.togettoyou.com:27051              ││
│  │  └── org3 → peer0.org3.togettoyou.com:47051              ││
│  └──────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### 7.2 关键代码实现

#### 7.2.1 Fabric 网关初始化

```go
func InitFabric() error {
    // 1. 初始化区块监听器
    initBlockListener("data/blocks")
    
    // 2. 为每个组织创建 gRPC 连接
    for orgName, orgConfig := range config.Organizations {
        // 2a. 创建 gRPC 连接 (TLS)
        conn := newGrpcConnection(orgConfig)
        
        // 2b. 创建身份 (MSP X.509 证书)
        id := newIdentity(orgConfig)
        
        // 2c. 创建签名函数 (私钥)
        sign := newSign(orgConfig)
        
        // 2d. 创建 Gateway 连接
        gw := client.Connect(id,
            client.WithSign(sign),
            client.WithClientConnection(conn))
        
        // 2e. 获取合约对象
        network := gw.GetNetwork("mychannel")
        contracts[orgName] = network.GetContract("mychaincode")
    }
}
```

#### 7.2.2 链码调用

```go
// 提交交易（写入数据）
func (s *Service) RegisterFarm(...) error {
    contract := fabric.GetContract("org1")
    result, err := contract.SubmitTransaction(
        "FarmContract:RegisterFarm",
        id, name, province, ...)
    // SubmitTransaction 执行完整交易流程：
    // 1. 构造提案 → 2. 发送给 Peer 背书 →
    // 3. 收集背书 → 4. 提交给 Orderer →
    // 5. 等待区块提交确认
}

// 查询交易（只读数据）
func (s *Service) QueryFarm(id string) error {
    contract := fabric.GetContract("org1")
    result, err := contract.EvaluateTransaction(
        "FarmContract:QueryFarm", id)
    // EvaluateTransaction 仅执行模拟阶段：
    // 1. 构造查询提案 → 2. 发送给 Peer →
    // 3. 返回查询结果（不经过 Orderer）
}
```

### 7.3 区块监听器设计

```go
func startBlockListener(orgName string) {
    // 1. 从上次中断的区块号继续
    lastBlockNum := getLastBlockNum(orgName)
    startBlock := lastBlockNum + 1
    
    // 2. 订阅区块事件
    events, err := network.BlockEvents(ctx, 
        client.WithStartBlock(startBlock))
    
    // 3. 循环处理新区块
    for block := range events {
        saveBlock(orgName, block)
        // 存储到 BoltDB
        // 键: "org1_19" → 值: BlockData JSON
    }
}
```

---

## 8. 前端架构设计

### 8.1 前端架构图

```
┌─────────────────────────────────────────────────────────────┐
│                   src/ 目录结构                               │
│                                                              │
│  main.ts          — 应用入口 (Pinia + Router)                │
│  App.vue          — 根组件 (<router-view>)                   │
│  style.css        — 全局样式                                 │
│                                                              │
│  ├── api/                                                    │
│  │   └── index.ts    — API 接口封装 (Axios)                  │
│  │                     agricultureApi / inspectionApi /       │
│  │                     supplyChainApi                        │
│  │                                                           │
│  ├── types/                                                  │
│  │   └── index.ts    — TypeScript 类型定义                    │
│  │                     Farm / Product / InspectionReport /    │
│  │                     LogisticsRecord / PurchaseOrder /      │
│  │                     TraceabilityInfo / BlockData           │
│  │                                                           │
│  ├── utils/                                                  │
│  │   ├── request.ts   — Axios 实例 + 拦截器                   │
│  │   ├── clipboard.ts — 剪贴板工具                            │
│  │   ├── common.ts    — 状态映射 (getStatusText/Color)       │
│  │   ├── random.ts    — 随机数据生成 (49种云南特产)           │
│  │   └── index.ts     — 统一导出                              │
│  │                                                           │
│  ├── router/                                                 │
│  │   └── index.ts    — 路由配置                              │
│  │                     / → Home                              │
│  │                     /agriculture → Agriculture            │
│  │                     /inspection → Inspection              │
│  │                     /supply-chain → SupplyChain           │
│  │                     /blockchain-viz → BlockchainViz       │
│  │                                                           │
│  └── views/                                                  │
│      ├── Home.vue           — 首页（Hero+卡片+流程+技术栈）   │
│      ├── Agriculture.vue    — 农业局面板（4 Tab）             │
│      ├── Inspection.vue     — 检测中心面板（2 Tab）           │
│      ├── SupplyChain.vue    — 供应链面板（4 Tab）             │
│      └── BlockchainViz.vue  — 链上可视化（深色主题）          │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 前端数据流

```
用户操作 → Vue 组件 → API 调用 → Axios → 后端 API → Fabric → 区块链
                                                        ↓
用户界面 ← Vue 响应式 ← JSON 解析 ← Axios 拦截 ← JSON 响应
```

### 8.3 Vite 代理配置

```typescript
// vite.config.ts
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8888',  // 后端地址
        changeOrigin: true,                // 跨域
      },
    },
  },
})
```

### 8.4 前端关键特性

| 特性 | 实现方式 | 说明 |
|------|----------|------|
| ID 复制 | `copyToClipboard()` + `CopyOutlined` 图标 | 鼠标悬停显示复制按钮 |
| 区块查看 | 右侧悬浮图标 + Drawer 抽屉 | 显示实时区块哈希 |
| 状态筛选 | RadioGroup + watch 监听 | 按状态过滤列表 |
| 精确搜索 | InputSearch + 远程查询 | 输入 ID 精确查找 |
| 链上可视化 | 独立页面 /blockchain-viz | 实时数据驱动 |
| 滚动动画 | IntersectionObserver | 元素进入视口时触发 |
| 响应式 | CSS Media Query | 适配手机/平板/桌面 |

---

## 9. 搭建过程与踩坑记录

### 9.1 环境搭建步骤

```
步骤 1: 安装依赖
├── Docker: docker --version (v24+)
├── Docker Compose: docker-compose --version (v2.x)
├── Go: go version (1.23+)
├── Node.js: node --version (18+)
└── npm: npm --version (9+)

步骤 2: 启动 Fabric 网络
└── cd network && ./install.sh
    ├── 步骤1-3: 环境检查 + CLI 容器
    ├── 步骤4: cryptogen 生成证书
    ├── 步骤5-7: configtxgen 配置
    ├── 步骤8: 启动 9个Docker容器
    ├── 步骤9-11: 创建通道 + 加入节点
    ├── 步骤12-13: 打包 + 安装链码
    ├── 步骤14-15: 3组织批准 + 提交
    └── 步骤16: InitLedger + Hello 验证

步骤 3: 启动后端
└── cd application/server && go run main.go
    ├── 加载配置 config.yaml
    ├── 初始化 Fabric 网关 (3组织连接)
    ├── 启动区块监听器 (BoltDB)
    └── 启动 Gin HTTP 服务 (:8888)

步骤 4: 启动前端
├── cd application/web && npm install
└── npm run dev → Vite (:5173)

步骤 5: 访问系统
└── http://localhost:5173
```

### 9.2 踩坑记录

#### 坑1: Permission denied（权限问题）

**现象**：
```
Error: open data/blocks/blocks.db: permission denied
Error: open crypto-config/.../priv_sk: permission denied
```

**原因分析**：
- Docker 和 `cryptogen` 以 `root` 用户创建文件
- `sudo ./install.sh` 也会导致后续文件归属 root
- 当前用户 `syh` 无读写权限

**排查过程**：
1. `ls -la data/blocks/` → 显示 `root root`
2. `ls -la crypto-config/` → 同样 `root root`
3. 确认是 Docker 容器内生成的文件的权限问题

**解决方案**：
```bash
sudo chown -R $USER:$USER network/data/
sudo chown -R $USER:$USER network/crypto-config/
sudo chown -R $USER:$USER network/config/
sudo chown -R $USER:$USER application/server/data/
```

**预防措施**：
- 不要在 `install.sh` 前使用 `sudo`
- 或者在脚本中自动执行 `chown`

#### 坑2: 端口占用 (address already in use)

**现象**：
```
Error: listen tcp :8888: bind: address already in use
```

**原因分析**：
- 上次启动的后端进程未正常退出
- 前端 Vite 进程也可能占用端口

**解决方案**：
```bash
# 查找并杀死占用端口的进程
lsof -ti:8888 | xargs -r kill -9
lsof -ti:5173 | xargs -r kill -9
```

#### 坑3: CLI 容器 No such exec instance

**现象**：
```
Error response from daemon: No such exec instance: cebec55cb4f...
```

**原因分析**：
- `install.sh` 在步骤 3 启动了 CLI 容器
- 但容器在某些条件下会退出（如 Docker 服务重启）
- 后续步骤的 `docker exec` 找不到已退出的容器

**解决方案**：
```bash
# 重新启动容器
docker start cli.togettoyou.com
# 或重新运行 install.sh
./install.sh
```

#### 坑4: InitLedger 未找到

**现象**：
```
Function InitLedger not found in contract FarmContract
```

**原因分析**：
- 原链码有 `InitLedger` 方法
- 重写链码时删除了该方法
- `install.sh` 步骤 16 调用 `InitLedger` 来初始化

**解决方案**：
```go
// 在 FarmContract 中添加 InitLedger
func (c *FarmContract) InitLedger(ctx contractapi.TransactionContextInterface) error {
    log.Println("云岭天眼溯源链码 InitLedger")
    return nil
}
```

#### 坑5: Hello 验证失败

**现象**：
```
[ERROR] network 未部署成功
```
但实际 `status:200` 表明初始化成功。

**原因分析**：
- `install.sh` 中验证代码：
  ```bash
  if ... grep "hello"; then
      log_success "部署成功"
  fi
  ```
- 新版链码 `Hello` 返回 `"云岭天眼高原特色农产品溯源系统 v1.0"`
- 不包含 `"hello"` 子串，grep 匹配失败

**解决方案**：
```bash
# 在 install.sh 中修改验证条件
if ... grep -E "云岭天眼|status:200"; then
```

#### 坑6: ProductOutlined 图标不存在

**现象**：
前端控制台报错：
```
SyntaxError: The requested module does not provide an export named 'ProductOutlined'
```

**原因分析**：
- `@ant-design/icons-vue` 库没有 `ProductOutlined` 图标
- Agriculture.vue 中错误引用

**解决方案**：
```typescript
// 将 ProductOutlined 替换为 AppstoreOutlined
import { PlusOutlined, AppstoreOutlined, ApartmentOutlined } from '@ant-design/icons-vue';
```

#### 坑7: BoltDB 数据库锁定 (timeout)

**现象**：
```
初始化区块监听器失败: 打开数据库失败：timeout
```

**原因分析**：
- BoltDB 同一时间只允许一个进程打开数据库文件
- 上一个 `go run main.go` 进程异常退出未释放锁
- 或残留进程仍在运行

**解决方案**：
```bash
# 删除锁定的数据库文件
rm -f application/server/data/blocks/blocks.db

# 或查找残留进程
lsof application/server/data/blocks/blocks.db
```

#### 坑8: docker-compose 命令不存在

**现象**：
```
docker: unknown command: docker compose
```

**原因分析**：
- Ubuntu 24.04 使用 `docker-compose`（带横线）而非 `docker compose`
- Docker 版本较旧，未集成 `compose` 子命令

**解决方案**：
```bash
# 使用 docker-compose（带横线）
docker-compose up -d
# 或安装新版本 Docker
sudo apt install docker-compose-v2
```

#### 坑9: SSH 连接 GitHub 失败

**现象**：
```
git@github.com: Permission denied (publickey).
Connection reset by 20.205.243.166 port 22
```

**原因分析**：
- 未配置 SSH 公钥
- 或端口 22 被网络封锁

**解决方案**：
```bash
# 1. 生成 SSH Key
ssh-keygen -t ed25519 -C "your@email.com"

# 2. 添加公钥到 GitHub
cat ~/.ssh/id_ed25519.pub
# 复制内容 → https://github.com/settings/ssh/new

# 3. 或使用 HTTPS + Token
git remote set-url origin https://github.com/User/Repo.git
git push -u origin main
# 输入 GitHub Personal Access Token
```

#### 坑10: GitHub Actions 自动构建失败

**现象**：
```
Run docker/login-action@v1
Error: Username and password required
```

**原因分析**：
- 原项目遗留的 `.github/workflows/build.yml`
- 尝试构建 Docker 镜像并推送到 `togettoyou/fabric-realty`
- 新仓库没有配置 Docker Hub 密钥

**解决方案**：
```bash
# 删除旧的 workflow 文件
rm -rf .github/workflows/
git commit -m "chore: remove legacy CI workflow"
git push
```

---

## 10. 教师可能提问的问题集

### 10.1 区块链相关

#### Q1: 什么是 Hyperledger Fabric？它和以太坊有什么区别？

**A**: Hyperledger Fabric 是一个**许可制（Permissioned）**的区块链框架，由 Linux 基金会托管。与以太坊的区别：

| 维度 | Fabric | 以太坊 |
|------|--------|--------|
| 网络类型 | 联盟链（许可制） | 公链（无许可） |
| 身份认证 | MSP + X.509 证书 | 钱包地址（匿名） |
| 共识机制 | Raft (CFT) | PoS (BFT) |
| 智能合约语言 | Go/Java/Node.js | Solidity |
| 交易流程 | Execute-Order-Validate | Order-Execute |
| 数据隐私 | 通道隔离 | 全局公开 |
| 代币 | 无原生代币 | ETH |
| 性能 | 万级 TPS | ~30 TPS |

#### Q2: Fabric 的交易流程是什么？

**A**: Fabric 采用 **Execute-Order-Validate** 三阶段模型：

1. **Execute（执行）**：客户端向 Peer 提交交易提案，Peer 模拟执行链码
2. **Order（排序）**：客户端收集背书后提交给 Orderer，Orderer 集群通过 Raft 共识排序并打包区块
3. **Validate（验证）**：Orderer 将区块分发给所有 Peer，Peer 验证背书并提交到账本

#### Q3: 什么是背书策略？本系统如何设计的？

**A**: 背书策略指定哪些组织的 Peer 需要对交易进行背书签名。本系统的背书策略：

- **FarmContract**: Org1 必须背书（仅农业局可注册农场）
- **InspectionContract**: Org2 必须背书（仅检测中心可创建报告）
- **ProductContract**: Org1 或 Org3 背书（农业局或供应链平台）
- **TraceContract**: 任一组织背书（所有组织可查询）

#### Q4: 通道（Channel）的作用是什么？

**A**: 通道是 Fabric 中实现数据隔离的机制：
- 每个通道拥有独立的账本和链码
- 只有加入通道的组织才能访问通道数据
- 本系统使用 `mychannel` 作为业务通道，三个组织共享

#### Q5: Fabric 的世界状态（World State）是什么？

**A**: 世界状态是账本中所有键值对的最新值数据库：
- 存储方式：LevelDB（键值存储）或 CouchDB（支持富查询）
- 本系统使用 LevelDB
- 每次交易提交时更新世界状态
- 查询操作直接读取世界状态，无需遍历区块链

#### Q6: 为什么选择 Raft 而不是 PBFT？

**A**: 
1. **联盟链场景**：节点是可信的，无需容忍拜占庭故障
2. **实现简单**：Raft 比 PBFT 容易理解和实现
3. **性能足够**：3 节点集群 Raft 性能完全满足需求
4. **成熟可靠**：etcd/Raft 是生产环境验证的成熟实现

#### Q7: Raft 如何处理节点宕机？

**A**: Raft 的故障恢复机制：
1. **Leader 宕机**：Follower 检测到选举超时后发起新选举
2. **Follower 宕机**：Leader 持续发送心跳，Follower 恢复后自动同步
3. **多数派存活**：3 节点集群可容忍 1 个节点宕机

#### Q8: 什么是 MSP？本系统如何配置？

**A**: MSP（Membership Service Provider）管理组织身份和权限：
- **Org1MSP**：农业局
- **Org2MSP**：检测中心  
- **Org3MSP**：供应链平台
每个 MSP 有独立的 CA 证书、TLS 证书、管理员证书

#### Q9: 如果 Orderer 全部宕机会怎样？

**A**: 
1. 网络无法产生新区块
2. 已有的 Peer 仍然可处理查询
3. 重启 Orderer 后恢复正常（账本持久化在磁盘）
4. 关键：Orderer 无状态，故障恢复快

#### Q10: 如何保证数据不可篡改？

**A**: 通过哈希链保证：
1. 每个区块包含前一个区块的哈希值
2. 修改任一区块会导致后续所有区块哈希失效
3. 每个 Peer 都保存完整账本，篡改需攻破大多数节点
4. 区块监听器实时监控区块变化

### 10.2 后端相关

#### Q11: 为什么使用 Gin 框架？

**A**: 
1. **高性能**：基于 httprouter，路由匹配快
2. **中间件丰富**：日志、跨域、恢复等内置
3. **社区活跃**：Go Web 框架中 Star 最多
4. **轻量简洁**：API 设计直观，学习成本低

#### Q12: 后端如何与 Fabric 网络交互？

**A**: 通过 `fabric-gateway` SDK：
```go
// 1. 初始化 gRPC 连接（TLS加密）
conn := grpc.Dial(peerEndpoint, grpc.WithTransportCredentials(creds))

// 2. 创建 Gateway 连接
gw := client.Connect(id, client.WithSign(sign), client.WithClientConnection(conn))

// 3. 获取合约对象
contract := gw.GetNetwork("mychannel").GetContract("mychaincode")

// 4. 调用链码
// 写入: contract.SubmitTransaction("FarmContract:RegisterFarm", ...)
// 读取: contract.EvaluateTransaction("FarmContract:QueryFarm", ...)
```

#### Q13: 多个组织如何共用一个后端？

**A**: 后端为每个组织分别创建独立的 Gateway 连接：
```go
// 为 org1、org2、org3 各创建一个合约客户端
contracts["org1"] = network.GetContract("mychaincode")
contracts["org2"] = network.GetContract("mychaincode")
contracts["org3"] = network.GetContract("mychaincode")

// 按需获取对应组织的合约
func GetContract(orgName string) *client.Contract {
    return contracts[orgName]
}
```
每个组织连接使用不同的证书，模拟不同组织的身份。

#### Q14: 区块事件监听是如何实现的？

**A**: 
1. 后端启动时为每个组织订阅区块事件
2. 收到新区块时，计算区块哈希并存入 BoltDB
3. BoltDB 以 `组织名_区块号` 为键存储
4. 提供分页查询接口给前端展示

#### Q15: ExtractErrorMessage 的作用？

**A**: 从 gRPC 错误中提取人类可读的错误信息：
```go
func ExtractErrorMessage(err error) string {
    st, ok := status.FromError(err)
    // 从 gRPC 状态中提取错误码和消息
    // 链码中的 fmt.Errorf 会传递到 gRPC 错误中
}
```

### 10.3 前端相关

#### Q16: 为什么使用 Vue 3 而不是 React？

**A**: 
1. **学习曲线低**：Vue 模板语法更接近 HTML
2. **组合式 API**：Vue 3 Composition API 逻辑复用灵活
3. **Ant Design Vue**：成熟的企业级组件库
4. **Vite**：极速热更新，开发体验好
5. **轻量**：Vue 运行时更小，加载更快

#### Q17: 什么是 Vite？为什么用它？

**A**: Vite 是新一代前端构建工具：
- **开发时**：利用浏览器原生 ES Module，无需打包，秒级启动
- **热更新**：修改代码后毫秒级更新
- **构建时**：使用 Rollup 打包，优化生产代码
- **代理配置**：内置开发服务器代理解决跨域

#### Q18: 前端如何解决跨域问题？

**A**: 通过 Vite 代理配置：
```typescript
// vite.config.ts
server: {
    proxy: {
        '/api': {
            target: 'http://localhost:8888',
            changeOrigin: true,
        },
    },
}
```
开发时前端请求 `/api/xxx` 自动代理到 `localhost:8888/api/xxx`。

#### Q19: TypeScript 的作用？

**A**: 
1. **类型检查**：编译时发现类型错误
2. **IDE 提示**：代码补全、参数提示
3. **代码可读性**：类型即文档
4. **减少运行时错误**：很多错误在编译期就被发现

#### Q20: 前端如何展示实时区块链数据？

**A**: 
1. `BlockchainViz.vue` 页面通过 API 获取真实数据
2. 使用 `setInterval(refreshAll, 30000)` 每 30 秒自动刷新
3. 展示实时区块哈希、交易数量、时间戳
4. 数据驱动而非静态模拟

### 10.4 环境与部署相关

#### Q21: 项目的运行环境要求？

**A**: 
| 工具 | 版本 | 用途 |
|------|------|------|
| Docker | 20.10+ | 运行 Fabric 网络容器 |
| Docker Compose | 2.x+ | 编排多容器 |
| Go | 1.23+ | 编译链码和后端 |
| Node.js | 18+ | 运行前端 |
| npm | 9+ | 管理前端依赖 |

#### Q22: 如何启动整个系统？

**A**: 
```bash
# 1. 启动 Fabric 网络
cd network && ./install.sh

# 2. 启动后端
cd application/server && go run main.go

# 3. 启动前端
cd application/web && npm run dev
```

#### Q23: 启动时最常见的错误及解决？

**A**: 见第 9 章节详细踩坑记录。最常见问题：
1. **Permission denied** → `sudo chown -R $USER:$USER`
2. **端口占用** → `lsof -ti:8888 | xargs kill -9`
3. **BoltDB timeout** → `rm -f data/blocks/blocks.db`

#### Q24: 如何重置整个系统？

**A**: 
```bash
# 1. 停止并清理 Fabric 网络
cd network
docker-compose down --volumes --remove-orphans

# 2. 删除数据文件
sudo rm -rf data/ config/ crypto-config/

# 3. 重启网络
./install.sh

# 4. 重启后端
cd application/server && go run main.go

# 5. 重启前端
cd application/web && npm run dev
```

#### Q25: 如何查看区块链上的数据？

**A**: 三种方式：
1. **Web 界面**：每个页面右侧的区块图标
2. **API 调用**：`curl http://localhost:8888/api/agriculture/block/list`
3. **CLI 命令**：`docker exec cli.togettoyou.com peer chaincode query ...`

#### Q26: 链码更新后如何部署？

**A**: 
```bash
# 1. 修改链码代码
cd chaincode
# 编辑 chaincode.go

# 2. 重新编译验证
go build

# 3. 重新部署
cd network
./install.sh
```

#### Q27: 数据存在哪里？

**A**: 
| 数据类型 | 存储位置 | 存储方式 |
|----------|----------|----------|
| 区块链账本 | `network/data/peer*.togettoyou.com/` | LevelDB |
| 世界状态 | `network/data/peer*.togettoyou.com/` | LevelDB |
| 区块监听 | `application/server/data/blocks/blocks.db` | BoltDB |
| 证书密钥 | `network/crypto-config/` | 文件系统 |
| 通道配置 | `network/config/` | 文件系统 |

#### Q28: 如何让外网访问？

**A**: 修改 Vite 配置允许外部访问：
```typescript
// vite.config.ts
server: {
    host: '0.0.0.0',  // 允许外部访问
    port: 5173,
}
```
然后通过 `http://服务器IP:5173` 访问。

---

## 11. 术语表

| 术语 | 英文 | 解释 |
|------|------|------|
| **联盟链** | Consortium Blockchain | 多个组织共同维护的许可制区块链 |
| **智能合约** | Smart Contract | 运行在区块链上的自动执行程序 |
| **链码** | Chaincode | Fabric 中的智能合约 |
| **Peer** | Peer | 维护账本的节点 |
| **Orderer** | Orderer | 排序节点，负责共识 |
| **通道** | Channel | 数据隔离的私有通信机制 |
| **MSP** | Membership Service Provider | 成员身份服务管理 |
| **CA** | Certificate Authority | 证书颁发机构 |
| **背书** | Endorsement | Peer 对交易的签名确认 |
| **共识** | Consensus | 节点间达成一致的算法 |
| **Raft** | Raft | 崩溃容错共识算法 |
| **CFT** | Crash Fault Tolerant | 崩溃容错 |
| **BFT** | Byzantine Fault Tolerant | 拜占庭容错 |
| **世界状态** | World State | 账本最新状态数据库 |
| **复合键** | Composite Key | Fabric 中组合多个字段的键 |
| **MVCC** | Multi-Version Concurrency Control | 多版本并发控制 |
| **gRPC** | gRPC | Google 开源的高性能 RPC 框架 |
| **Gin** | Gin | Go 语言的 Web 框架 |
| **Vite** | Vite | 新一代前端构建工具 |
| **BoltDB** | BoltDB | Go 实现的嵌入式 KV 数据库 |
| **SDK** | Software Development Kit | 软件开发工具包 |
| **API** | Application Programming Interface | 应用程序接口 |
| **REST** | Representational State Transfer | RESTful API 风格 |
| **TLS** | Transport Layer Security | 传输层安全协议 |

---

> **文档结束**
> 
> 云岭天眼 · 高原特色农产品区块链溯源系统  
> 最后更新: 2026-06-27
